# 交付：R5 Sileo 默认预选 + R14 固定源预置（C 侧层1）

- 交付人：并行搜索员C　初版 18:45（四/五源版）→ **v2 定稿 23:35（roothide 参照系，V0.9.0 就绪）**
- 任务出处：SSOT（01-需求追溯）执行状态 R14/Sileo 预选行；ADR-R14 层1；SSOT v2.13 七源终版
- 口径演进：18:32"和多巴胺一样"→18:43"五个源"→18:50"去看 roothide 多巴胺"→**v2.13 七源架构（roothide 对齐）**

---

## 一、R5：Sileo 默认预选（✅ 已交付）

### 改动 1：`Application/Euphoria/UI/EUUIManager.m`

新增默认项常量（文件头，L15-16）：

```objc
// R5/R15：首装包管理器默认预选项（与 PkgManagers.plist 的 Key 对应）
static NSString * const EUDefaultPackageManagerKey = @"org.coolstar.SileoStore";
```

`enabledPackageManagerKeys`（L144-158）改前 → 改后：

```objc
// 改前
NSArray *enabledPkgManagers = [_preferenceManager preferenceValueForKey:@"enabledPkgManagers"] ?: @[];

// 改后
// R5/R15: 首次运行（偏好未写入）时 Sileo 默认预选；
// 用户在首装页主动取消后偏好为非 nil 数组，走用户选择，不在此覆盖。
NSArray *enabledPkgManagers = [_preferenceManager preferenceValueForKey:@"enabledPkgManagers"];
if (!enabledPkgManagers) {
    enabledPkgManagers = @[EUDefaultPackageManagerKey];
}
```

### 行为说明
- 首装未做过任何选择（偏好为 nil）→ Sileo 默认勾选，Continue 按钮直接可用，开箱即装（R5"捆绑 sileo"语义补完）。
- 用户主动取消 Sileo → 偏好写入非 nil 数组（可为空），下次进入**不会**回弹重选（尊重用户选择）。
- `resetPackageManagers`（设置页重置）删除偏好键 → 回到默认预选态，语义自洽。
- 链路核查：选择页（EUPkgManagerPickerView L42/L116）、安装链（EUBootstrapper installPackageManagers L533→enabledPackageManagers）、越狱按钮（EUJailbreakButton L79/L115）全部经 `enabledPackageManagerKeys` 读取，**无旁路读取点**，单点改动即全覆盖。

---

## 二、R14 层1：固定源预置（✅ **roothide 参照系版 v2 已实施（23:35，V0.9.0 就绪）**；chflags 重放待 B 接线）

> 版本演进：18:42 四源版（Dopamine 对齐）→ 18:50 五源版（+Procursus 上游）→ **23:35 v2 roothide 参照系版**（SSOT v2.13 七源终版口径，用户 18:50:08"去看roothide多巴胺"纠偏后定稿）。

### 改动 2：`Application/Euphoria/Resources/PresetSources.plist`（清单单点，v2 全文）

写入 sources.list.d 的五源（第六槽 EU 自家源待 R17 域名即加，第七源 bootstrap 分发源自持不落盘——"六源写入+分发源自持"v6 口径）：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<array>
        <dict>
                <key>Key</key><string>yourepo</string>
                <key>URL</key><string>https://yourepo.com/</string>
                <key>Suites</key><string>./</string>
                <key>Components</key><string></string>
                <key>Fixed</key><true/>
        </dict>
        <dict>
                <key>Key</key><string>chariz</string>
                <key>URL</key><string>https://repo.chariz.com/</string>
                <key>Suites</key><string>./</string>
                <key>Components</key><string></string>
                <key>Fixed</key><true/>
        </dict>
        <dict>
                <key>Key</key><string>havoc</key>
                <key>URL</key><string>https://havoc.app/</string>
                <key>Suites</key><string>./</string>
                <key>Components</key><string></string>
                <key>Fixed</key><true/>
        </dict>
        <dict>
                <key>Key</key><string>bigboss</string>
                <key>URL</key><string>http://apt.thebigboss.org/repofiles/cydia/</string>
                <key>Suites</key><string>stable</string>
                <key>Components</key><string>main</string>
                <key>Fixed</key><true/>
        </dict>
        <dict>
                <key>Key</key><string>roothideproc</string>
                <key>URL</key><string>https://roothide.github.io/procursus</string>
                <key>Suites</key><string>iphoneos-arm64e/{BOOTSTRAP}</string>
                <key>Components</key><string>main</string>
                <key>Fixed</key><true/>
        </dict>
</array>
</plist>
```

- 名单逐字对齐 roothide Dopamine 1.x default.sources 六节（B20 实证 Bootstrapper.swift:334-364；A_R14R17 文档 URL 互证）。

- **替代关系（相对上游 Dopamine 四源）**：无 ElleKit（其位置→roothide 官方源=EU 自家源占位）、无 apt.procurs.us（→roothide 自建镜像）、新增 YouRepo（B20 表格逐项对应）。
- **roothide Procursus 镜像 suite=占位符 `iphoneos-arm64e/{BOOTSTRAP}`**：C 23:30 实测裁决——`dists/iphoneos-arm64e/1800/Release` 返回 200、`/17/Release` 404 → **suite=bootstrap 代数（非系统大版本；B20 正确，A 文档 %d 口径已修正）**。写入时由 `writePresetSources` 以 `[self bootstrapVersion]`（与本次解包 tar 同源取值）实时替换，1800/1900 代数自适应。
- Sileo 首启自播与显式预置同 URL 按 URL 去重；stock Sileo 播种的是 apt.procurs.us（不在预置清单）——Sileo-roothide fork 是否保留该播种待实机核（B20 未决项），若保留仅多一条上游源，属可接受偏差，不阻塞 V0.9.0。

### 改动 3：`Application/Euphoria/Jailbreak/EUBootstrapper.m`

原 L394-413 硬编码 default.sources 写入（20 行）替换为：

```objc
// R14（ADR-R14 层1 / SSOT v2.13 七源终版）：预置固定源，清单单点 PresetSources.plist
// （roothide 参照系：YouRepo/Chariz/Havoc/BigBoss/roothide Procursus 镜像；
//  EU 自家源待 R17 域名单点即加；bootstrap 分发源自持不落盘。）
// 每条固定源一个 <key>.list（单行 deb 格式，Sileo/Zebra 通用）；
// 先清旧 default.sources 与清单自管 .list 再写，幂等且防重复源。
// 不可删锁定（chflags UF_IMMUTABLE 挂载后幂等重放）为层2，由 BaseBin 侧执行。
[self writePresetSources];
```

新增两个方法（置于 installPackageManagers 前，L495-529）：

```objc
// R14（ADR-R14 层1 / SSOT v2.13 七源终版）：固定源清单单点
// （Application/Resources/PresetSources.plist，roothide 参照系）
// 写入 sources.list.d 五源=YouRepo/Chariz/Havoc/BigBoss/roothide Procursus 镜像
// （逐字对齐 roothide Dopamine 1.x default.sources，B20 实证）；
// 第六槽 EU 自家源=占 roothide 官方源位，域名随 R17 定案后单点即加；
// 第七源 bootstrap 分发源=GitHub releases 自持（非 apt 源，不落盘）。
- (NSArray*)presetSources
{
    static NSArray *sources = nil;
    static dispatch_once_t onceToken;
    dispatch_once(&onceToken, ^{
        NSString *path = [[NSBundle mainBundle] pathForResource:@"PresetSources" ofType:@"plist"];
        sources = path ? [NSArray arrayWithContentsOfFile:path] : @[];
    });
    return sources;
}

// R14（ADR-R14 层1）：bootstrap 解包完成后写预置源到 /var/jb/etc/apt/sources.list.d/
// 每条固定源一个 <key>.list（单行 deb 格式，Sileo/Zebra 均识别）；
// {BOOTSTRAP} 占位符 → [self bootstrapVersion]（与本次解包 tar 同源取值），
// 保证 roothide Procursus 镜像 suite 永远匹配实际 bootstrap 代数（1800/1900）。
// 幂等：先移除上游遗留 default.sources 与本清单自管的 *.list，再全量重写（防重复源）；
// 用户自建源文件不受影响。不可删锁定（chflags UF_IMMUTABLE 挂载后重放）为 ADR-R14 层2。
- (void)writePresetSources
{
    NSFileManager *fm = [NSFileManager defaultManager];
    NSString *sourcesDir = JBROOT_PATH(@"/etc/apt/sources.list.d");
    [fm createDirectoryAtPath:sourcesDir withIntermediateDirectories:YES attributes:nil error:nil];

    [fm removeItemAtPath:[sourcesDir stringByAppendingPathComponent:@"default.sources"] error:nil];

    for (NSDictionary *source in [self presetSources]) {
        NSString *fileName = [NSString stringWithFormat:@"%@.list", source[@"Key"]];
        [fm removeItemAtPath:[sourcesDir stringByAppendingPathComponent:fileName] error:nil];

        NSString *url = [source[@"URL"] stringByReplacingOccurrencesOfString:@"{BOOTSTRAP}" withString:[self bootstrapVersion]];
        NSString *suites = [source[@"Suites"] stringByReplacingOccurrencesOfString:@"{BOOTSTRAP}" withString:[self bootstrapVersion]];
        NSString *line = [NSString stringWithFormat:@"deb %@ %@ %@\n", url, suites, source[@"Components"] ?: @""];
        [line writeToFile:[sourcesDir stringByAppendingPathComponent:fileName] atomically:YES encoding:NSUTF8StringEncoding error:nil];
    }
}
```

写入时机=bootstrap 解包完成回调（原 default.sources 同位置）→ 早于 rootful 流 bootstrapfs 打包，满足 ADR"解包后、create 前"要求。

### 改动 4：`Application/Euphoria.xcodeproj/project.pbxproj`（资源注册，4 处）

- PBXBuildFile：`E14C00022B74B65900E17924 /* PresetSources.plist in Resources */`
- PBXFileReference：`E14C00012B74B65900E17924 /* PresetSources.plist */`（text.plist.xml）
- Resources 组 children + Resources 构建相位各 1 处（随 sileo.deb 条目旁）

---

## 三、验证证据（完整证据链）

1. **plist 合法性**：python plistlib 解析通过——PresetSources.plist **v2 共 5 条目**（yourepo/chariz/havoc/bigboss/roothideproc，URL/Suite/Components 与 roothide Dopamine default.sources 逐字一致，占位符 {BOOTSTRAP} 待运行时替换）；PkgManagers.plist 2 条目对照无损。
2. **bootstrap 增证（下载实测）**：`https://apt.procurs.us/bootstraps/1800/bootstrap-iphoneos-arm64.tar.zst`（20.8MB）下载解包全量检索：唯一命中 `var/jb/usr/share/man/man5/sources.list.5.zst`（man 手册页）——**bootstrap 不携带任何源条目**。
3. **deb 增证（control.tar 一并核过）**：sileo.deb postinst 仅做 checkra1n 残留清理 + uicache 触发，**不写源**；zebra.deb preinst 无源操作。
4. **⭐ 第五源机制定案（用户"五源"指正的实证）**：sileo.deb data.tar 内 `Sileo.app/Sileo` 二进制内嵌默认源 URL（`https://repo.chariz.com`、`https://apt.procurs.us/`、`http://apt.thebigboss.org/repo…`）——**Procursus 第五源由 Sileo 本体首启自播**，非 bootstrap 自带、非 deb 脚本写入。
   → **"和多巴胺一样"五源 = App 写 4（default.sources）+ Sileo 自播 1（procursus.sources）**。本实现复刻 App 侧 4 源写入，Sileo 默认预选（R5）→ 首启自播第五源，端到端与 Dopamine 结果一致；procursus.sources 由 Sileo 创建，不在我"先清"名单内（清的是遗留 default.sources 与自管 `*.list`），重装/重越狱不丢。
   → Zebra 无自播行为，见 4 源——与上游 Dopamine+Zebra 行为一致（同为 default.sources 4 源），口径不偏。
5. **pbxproj 注册**：两 UUID 共 4 行命中（定义/fileRef/组/相位），无冲突。

## 四、七源架构映射与遗留接线点（非本交付）

**SSOT v2.13 七源终版 → 本实现的映射**：

| # | 七源槽位 | v2 现状 |
|---|---|---|
| 1-4 | 公共四源 Chariz/Havoc/BigBoss/YouRepo | ✅ 已写入（plist yourepo/chariz/havoc/bigboss） |
| 5 | roothide 官方源位 → **EU 自家源占位** | ⏳ 域名随 R17 自研 PM 命名定案后 plist 单点即加（零代码） |
| 6 | roothide Procursus 镜像（suite 随代数动态） | ✅ 已写入（roothideproc，{BOOTSTRAP} 占位符） |
| 7 | bootstrap 分发源 | ✅ 设计上自持不落盘（GitHub releases，非 apt 源） |

- **zh_CN 镜像（iosjb.top 主源+procursus 镜像，roothide :427-442 先例）**：团队自决决策点。C 建议 V0.9.0 后置（实现成本低：locale 判定+plist 增两元数据开关即可），待拍板。
- **Zebra 容器写法**：roothide 对 Zebra 单独写 `xyz.willy.Zebra/sources.list`（单行 6 条含 getzbra.com，:366-375）——B 按 ADR-R14 18:55 先例接线（需运行时解析 Zebra App Group 容器 UUID，属 BaseBin 侧逻辑）；本实现 sources.list.d 通用五源 Zebra 亦可读，不阻塞。
- **B（层2 chflags）**：锁定对象=plist 全部 `<key>.list`（现 5 个，EU 源加入后 6 个）+ Sileo 自播的 procursus.sources（同属固定区，Sileo 重播同 URL 幂等）；挂在挂载完成后 root 上下文、每次越狱幂等重放。
- **Sileo-roothide fork 首启播种未决项**（B20）：stock Sileo 播种 apt.procurs.us+BigBoss——fork 是否保留待实机核，若保留仅多一条上游源，可接受偏差。
- **A 调研→R15**：Installer 无公开 rootless deb（A 18:49"不可捆绑"）→ 第三 PM 条目策略待定；Installer 若落地再验证其源存储是否走 sources.list.d。
- SSOT（R14 需求行+执行状态行）与 ADR-R14 §3 已同步 v2 状态；本文件随 18:45 首版一并补发（18:53 工具中断欠账，23:35 补齐）。
