# C28 修复线·静态审计批次一（App 侧核心流 + BaseBin 自研件结构层）

**日期**：2026-09-01 21:51 ~ 22:20
**执行**：并行搜索员C（汇总员 21:48 修复线指派，切片：App 核心流 + BaseBin 自研件；EUTrollE.m 归 B 全量复核，C 未做逻辑改动）
**触发**：用户 21:44/21:45「你们的代码漏洞很多 / 快点修复」

---

## 一、结论速览

| 级别 | 数量 | 状态 |
|---|---|---|
| P0（编译阻断/必崩） | 1 | **已修复并复扫清零** |
| P1（逻辑级确认缺陷） | 0 | — |
| 低危观察项 | 3 | 记录在案，批次二跟进 |
| 结构级验证通过的文件 | 62 App .m + 22 BaseBin 自研/改码件 | 全绿 |

**用户构建 r3 包必失败的主因已定位并清除**：EUSettingsController.m 四行终端吃字符残留（`[h[hideJbSpecifier …]`），该文件此前无法通过编译。修复后建议用户换 r4 包重试构建。

## 二、方法声明（本批次新工具，已沉淀可复用）

1. **结构级扫描器** `agents/6a8d0a5300ccbc1a3c89cebe/audit/struct_scan2.py`：括号平衡（带原始行号）+ 控制字符 + 编码残缺检测，针对本项目两次踩过的"终端吃字符"腐蚀家族。
2. **上游规范化 diff** `agents/6a8d0a5300ccbc1a3c89cebe/audit/fork_diff.py`：以 opa334/Dopamine **3.x 分支**（仓库默认分支，即我们的基线）为参照，品牌词归一（EU*→DO*、Euphoria→Dopamine、EUPH→DOPA）后统一 diff。**上游久经实机考验，任何非品牌差异=疑点**——这是 fork 审计的正解，避免了我在 EUJailbreaker.m 上的误报（见 §四）。

## 三、P0 修复明细

**文件**：`Application/Euphoria/UI/Settings/EUSettingsController.m` L356-359
**实况（od 取证）**：四行行首为 `[h[hideJbSpecifier setProperty:…];`——合法行应为 `[hideJbSpecifier …];`。残缺形态 `[h[` 判定为**此前一次"吃掉 `[`"事故的错误修复残留**（补的时候补成了 `[h[`，双括号+杂散 h）。
**影响**：ObjC 语法错误，编译必然失败；用户自建 r3 时 Application target 直接编不过——与「代码漏洞很多」反馈吻合。
**修复**：`[h[hideJbSpecifier setProperty:` → `[hideJbSpecifier setProperty:`（×4，Edit 全替换）。
**验证**：结构扫描器复扫该文件 0 issue；全树 `.m/.c/.h` 按腐蚀签名（`^\s*\[\w\[\w` 双括号 / 无开括号消息发送）复查 **0 残留**。
**位置背景**：正是 08-29 R36 落地的"rootless 隐/显越狱"开关区（该开关 8-28 曾报"按钮被吃"，两次事故同区）。

## 四、验证结论（上游 3.x 规范化 diff，7 文件 23 hunks 全定性）

| 文件 | hunks | 定性 |
|---|---|---|
| EUJailbreaker.m | **0** | 品牌归一后与上游**逐字节一致**。⚠️ 审计过程中 `flag &= P_SUGID`（elevatePrivileges）曾被怀疑为吃掉 `~`，**经上游原文比对确认该写法连同注释均为上游原样**——未改（非品牌改动不碰 Exploits/主流程的原则）。教训：改前必对照上游。 |
| EUExploitManager.m | **0** | 同上，逐字节一致。 |
| EUPreferenceManager.m | 1 | 品牌偏好文件名（随 bundle ID），一致。 |
| EUUpdateViewController.m | 1 | releases 链接指向占位仓库（已知项，发布前替换）。 |
| EUEnvironmentManager.m | 6 | 全部意图性（R34 三态/R37 模式判定/R38 隐藏档/R39 矩阵注释/idownloadd 品牌键）。`roothideUserEnabled`/`rootfulUserEnabled` 在 launchdhook jbsettings.c 双向注册 ✓（rootful→roothide 捆绑在服务端 118-122 行有第二层兜底）。 |
| EUBootstrapper.m | 7 | 全部意图性（R14 预置源+锁定/R19 ElleKit/R25 bootstrap 修正/R34 卸载加固/TrollE 钩子）。交叉验证全过：ellekit.deb 在 pbxproj Resources 阶段（拷至 bundle 根，与代码读取路径一致）；bootstrapVersion 15.x→1800 / 16+→1900 与镜像索引自洽；EUPHORIA 宏 define/引用一致。 |
| EUSettingsController.m | 8 | 全部意图性（08-28 五件修复 + B 的 R36/R37 + 主题门槛）。新增 spec 引用的 11 个选择器全部有实现（762-920 行）；`jbclient_jbsettings_get_bool`/`jbclient_platform_jbsettings_set_bool` 均为真实 API（jbclient_xpc.h L22/L29）；上游 hideUnhideJailbreakPressed 随其按钮一并移除，无悬空引用。 |

**App 侧 62 个 .m 全量结构扫描（含 EUTrollE.m、Exploits 九模块、全部 UI/控制器）：0 issue。**

## 五、关键链路实证（对"开关无效/假越狱"类反馈的排雷）

1. **aegisd 在 rootless 也活着**：BaseBin Makefile L97 `_external/basebin/*`（含 aegisd.plist）→ .build → L120 tar 成 `basebin/LaunchDaemons/`；launchdhook daemon_hook.m 把 `JBROOT/basebin/LaunchDaemons` 注入 launchd；plist RunAtLoad+KeepAlive ⇒ **aegis 后端不随 roothide 门控**，rootless"隐/显越狱"开关（R36：jbctl aegis add/remove 本体）链路成立。aegis.conf 在 /var/jb 持久化，重越狱后 aegisd 启动即恢复隐藏。
2. **jbctl aegis 子命令齐全**（main.m L358-385，status/enable/disable/set-level/add/remove/list/clear），App 调用参数形态 `@[@"aegis", add/remove, bundleID]` 匹配。
3. **卸载加固（R34）顺序正确**：bootout 守护→按可执行路径前缀 SIGKILL 残留→删树→（调用方）重启。防"删树后守护进程回写复活"。

## 六、低危观察项（不阻断，批次二处理）

1. `EUBootstrapper.m` R34 段 `daemonsDir` 用字面量 `/var/jb/Library/LaunchDaemons`，未从 `gSystemInfo.jailbreakInfo.rootPath` 派生——当前各模式 rootPath 均为 /var/jb 故等价，但 R46 路径随机化落地时此行是漏点。
2. 同段 `fork()` 失败分支未处理（best-effort bootout，影响极小）。
3. rootless 隐藏在未越狱态（重启后）自然失效（aegis 为越狱态机制），与上游"LS 层隐藏跨重启"语义不同——R36 已定案为设计取舍，提示 UI 文案已用 Footer 说明。

## 七、批次二 backlog

- BaseBin 自研件逻辑级深读（rootful.c / rootful_fakefs.c / cloak 全家 / jbdomain_euphoria.c / bootstrapfs 三态——结构级本批已全绿，逻辑级待深读）
- Localizable 26 语言键完整性比对（Settings 新键的翻译覆盖）
- PresetSources.plist 三模式段与 writePresetSources 代码的字段级比对
- B 线 EUTrollE.m 全量复核结论合流后出批次二

## 八、流程教训（入长期教训）

1. **工具结果显示层会吞 `[`+字母序列**（本批实锤两型：`[h`、`[m` 被吞——rg/Read/自研 scanner 输出均出现过）。凡涉及方括号邻接内容的判断，**必须 od -c 取原始字节**再下结论。
2. `[h[` 形态=错误修复残留：修"吃 `[`"事故时若只靠显示层确认（被吞字后看到的是假象），会把补丁打成残缺。**修复后必须复扫+od 复核**。
3. fork 审计先拉上游 3.x 对照（fork_diff.py），先排除上游自带的"怪写法"再定性——本批 EUJailbreaker P_SUGID 一案即为反例警示。
